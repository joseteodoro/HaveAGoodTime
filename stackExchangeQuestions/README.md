We are using rabbitmq to schedule the jobs and couchdb to store the json.

Base directory is stackExchangeQuestions.

`cd stackExchangeQuestions`

Start with `docker-compose up`

Stop with `docker-compose down`
